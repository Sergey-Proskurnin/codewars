// Я создал эту функцию, чтобы добавить пять к любому переданному ей числу и вернуть
// новое значение.Он не вызывает ошибок, но возвращает неправильный номер.

// Вы можете помочь мне исправить эту функцию?

function addFive(num) {
  return num + 5;
   ;
}

// describe('fix add five', () => {
//   it('fixed tests', () => {
//     Test.assertEquals(addFive(5), 10)
//     Test.assertEquals(addFive(0), 5)
//     Test.assertEquals(addFive(-5), 0)
//   })
// })

console.log((addFive(5), 10));
console.log((addFive(0), 5));
console.log((addFive(-5), 0));
